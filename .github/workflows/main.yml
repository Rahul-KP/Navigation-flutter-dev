
name: Build and Test

on:
  push:
    branches: [ "master" ]
    
jobs:
  build:
    name: Build APK & upload to Github Releases
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Flutter action
        uses: subosito/flutter-action@v2.8.0
        with:
          channel: 'stable'
          cache: true
          architecture: x64
      - run: |
          echo CACHE-PATH=${{ steps.flutter-action.outputs.CACHE-PATH }}
          echo CACHE-KEY=${{ steps.flutter-action.outputs.CACHE-KEY }}
          echo CHANNEL=${{ steps.flutter-action.outputs.CHANNEL }}
          echo VERSION=${{ steps.flutter-action.outputs.VERSION }}
          echo ARCHITECTURE=${{ steps.flutter-action.outputs.ARCHITECTURE }}
      - run: flutter pub get
      - run: flutter build apk --debug
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        with:
          tag_name: $(cat local.properties | cut -d "=" -f 2)
          release_name: Release $(cat local.properties | cut -d "=" -f 2)
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./build/app/outputs/apk/debug/app-debug.apk
          asset_name: Navigation.apk
          asset_content_type: application/apk
